---
дата: 2026-09-16
тип: входящее
статус: сырое
агент: человек
теги:
  - inbox
---

# Заметка 2026-09-16_08-48-28

# ПРАВИЛА ДЛЯ AIDER (.aider.conf.yml / CONVENTIONS.md)

1. УПРАВЛЕНИЕ ФАЙЛАМИ:
   - НИКОГДА не пытайся создавать, редактировать или патчить файлы с кодом через команды терминала (`cat`, `echo`, `touch`, `sed`, `python -c`).
   - Для создания и модификации файлов используй ТОЛЬКО встроенные инструменты редактирования Aider (native file editing formats / SEARCH-REPLACE blocks).

2. РАБОТА С BASH / КОМАНДАМИ:
   - Терминальные команды (bash/zsh) предназначены ИСКЛЮЧИТЕЛЬНО для:
     а) Запуска тестов (`pytest`, `npm test` и т.д.);
     б) Установки зависимостей (`pip install`, `brew install`);
     в) Запуска утилит проверки синтаксиса и линтеров (`ruff check`, `mypy`).

3. ИЗОЛЯЦИЯ СТРОК В СКРИПТАХ:
   - При написании скриптов автоматизации macOS избегай сложных вложенных строковых конструкций `osascript -e "..."`.
   - Передавай текст уведомлений и аргументы через переменные окружения (`os.environ`) или нативные библиотеки, чтобы исключить проблемы с кавычками.
